{
  "name": "@chookscord/logger",
  "version": "1.0.0",
  "description": "A small logging library for picocolors adding log levels.",
  "main": "dist/index.js",
  "types": "dist/typings/index.d.ts",
  "files": [
    "dist",
    "!**/*.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc --outDir dist",
    "clean-build": "rm -rf dist && yarn build",
    "dev": "yarn build --watch"
  },
  "dependencies": {
    "picocolors": "^1.0.0"
  },
  "repository": {
    "type": "git",
    "directory": "packages/logger",
    "url": "chookscord/framework"
  },
  "license": "MIT",
  "keywords": [
    "log",
    "logger",
    "logging"
  ],
  "publishConfig": {
    "access": "public"
  }
}
