{
  "name": "@chookscord/cli",
  "version": "0.5.2",
  "main": "dist/index.js",
  "repository": "https://github.com/chookscord/framework",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "yarn build -w",
    "build": "tsc",
    "clean-build": "rm -rf dist && yarn build && rm dist/.tsbuildinfo"
  },
  "dependencies": {
    "@chookscord/compiler": "^0.5.0",
    "@chookscord/lib": "^0.5.0",
    "discord.js": "^13.1.0",
    "dotenv": "^10.0.0"
  }
}
