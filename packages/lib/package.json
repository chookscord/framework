{
  "name": "@chookscord/lib",
  "version": "0.9.0",
  "main": "dist/index.js",
  "repository": "chookscord/framework",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npx tsc --outDir dist",
    "clean-build": "rm -rf dist && yarn build",
    "dev": "yarn build --watch"
  },
  "dependencies": {
    "@types/node-fetch": "^2.5.12",
    "chalk": "^4.1.2",
    "consola": "^2.15.3"
  },
  "peerDependencies": {
    "discord.js": "^13.1.0"
  }
}
