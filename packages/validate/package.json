{
  "name": "@chookscord/validate",
  "version": "0.3.0",
  "description": "Small validation utility for Discord application commands.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/chookscord/framework.git",
    "directory": "packages/validate"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npx tsc --outDir dist",
    "clean-build": "rm -rf dist && yarn build",
    "dev": "yarn build --watch"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
